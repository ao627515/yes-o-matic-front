import { DEFAULT_REQUEST } from './default-request.js';
import { AcceptButton } from '../models/accept-button.js';

export const DEFAULT_ACCEPT_BUTTON = [
  new AcceptButton(
    undefined,
    'Oui',
    null,
    null
  )
];